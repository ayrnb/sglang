[build-system]
requires = ["setuptools", "wheel", "torch"]
build-backend = "setuptools.build_meta"

[project]
name = "over_encoding_ops"
version = "0.1.0"
description = "Over Encoding Ops - Custom memory allocators for PyTorch tensors"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
